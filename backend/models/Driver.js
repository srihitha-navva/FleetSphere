import mongoose from 'mongoose';
const schema = new mongoose.Schema({ user: { type: mongoose.Schema.Types.ObjectId, ref: 'User', required: true, unique: true }, licenseNumber: { type: String, required: true, unique: true, uppercase: true }, licenseExpiry: { type: Date, required: true }, joiningDate: Date, emergencyContact: String, address: String, status: { type: String, enum: ['AVAILABLE', 'ASSIGNED', 'ON_TRIP', 'ON_LEAVE', 'INACTIVE'], default: 'AVAILABLE' }, assignedVehicle: { type: mongoose.Schema.Types.ObjectId, ref: 'Vehicle' }, organization: { type: mongoose.Schema.Types.ObjectId, ref: 'Organization', required: true, index: true }, branch: { type: mongoose.Schema.Types.ObjectId, ref: 'Branch', required: true, index: true } }, { timestamps: true });
export default mongoose.model('Driver', schema);

import mongoose from 'mongoose';
const schema = new mongoose.Schema({ vehicle: { type: mongoose.Schema.Types.ObjectId, ref: 'Vehicle', required: true }, maintenanceType: { type: String, enum: ['SERVICE', 'REPAIR', 'INSPECTION', 'TYRE', 'OIL_CHANGE', 'OTHER'], required: true }, serviceDate: { type: Date, required: true }, nextServiceDate: Date, currentOdometer: Number, nextServiceOdometer: Number, description: String, serviceProvider: String, partsCost: { type: Number, default: 0 }, laborCost: { type: Number, default: 0 }, totalCost: { type: Number, default: 0 }, status: { type: String, enum: ['SCHEDULED', 'IN_PROGRESS', 'COMPLETED', 'OVERDUE', 'CANCELLED'], default: 'SCHEDULED' }, invoice: String, notes: String, organization: { type: mongoose.Schema.Types.ObjectId, ref: 'Organization', required: true, index: true }, branch: { type: mongoose.Schema.Types.ObjectId, ref: 'Branch', required: true, index: true }, createdBy: { type: mongoose.Schema.Types.ObjectId, ref: 'User' } }, { timestamps: true });
schema.pre('validate', function calculate(next) { this.totalCost = (Number(this.partsCost) || 0) + (Number(this.laborCost) || 0); if (this.status === 'SCHEDULED' && this.serviceDate < new Date()) this.status = 'OVERDUE'; next(); });
export default mongoose.model('Maintenance', schema);

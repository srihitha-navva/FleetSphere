import mongoose from 'mongoose';
const schema = new mongoose.Schema({ registrationNumber: { type: String, required: true, uppercase: true, trim: true }, vehicleType: { type: String, required: true }, make: String, model: String, year: Number, color: String, fuelType: { type: String, enum: ['DIESEL', 'PETROL', 'ELECTRIC', 'CNG', 'HYBRID'], default: 'DIESEL' }, capacity: Number, odometer: { type: Number, default: 0, min: 0 }, status: { type: String, enum: ['AVAILABLE', 'ASSIGNED', 'ON_TRIP', 'MAINTENANCE', 'INACTIVE'], default: 'AVAILABLE' }, organization: { type: mongoose.Schema.Types.ObjectId, ref: 'Organization', required: true, index: true }, branch: { type: mongoose.Schema.Types.ObjectId, ref: 'Branch', required: true, index: true }, assignedDriver: { type: mongoose.Schema.Types.ObjectId, ref: 'Driver' }, insuranceNumber: String, insuranceExpiry: Date, registrationExpiry: Date, image: String, notes: String }, { timestamps: true });
schema.index({ organization: 1, registrationNumber: 1 }, { unique: true });
export default mongoose.model('Vehicle', schema);

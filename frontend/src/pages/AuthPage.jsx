import { useState } from 'react'; import { Link, useLocation, useNavigate } from 'react-router-dom'; import { useAuthStore } from '../store/authStore';
export default function AuthPage({ register = false }) { const navigate = useNavigate(); const location = useLocation(); const action = useAuthStore(register ? (s) => s.register : (s) => s.login); const [form, setForm] = useState({ name: '', email: '', password: '', phone: '' }); const [error, setError] = useState(''); const [saving, setSaving] = useState(false); const submit = async (e) => { e.preventDefault(); setSaving(true); setError(''); try { await action(form); navigate(location.state?.from?.pathname || '/dashboard'); } catch (err) { setError(err.message || 'Unable to continue'); } finally { setSaving(false); } }; return <div className="auth-page"><section className="auth-intro"><div className="brand"><span>F</span> FleetSphere</div><div><p className="eyebrow">FLEET OPERATIONS, SIMPLIFIED</p><h1>Every vehicle,<br />every mile, <em>in view.</em></h1><p>One secure workspace for drivers, dispatch, maintenance and finance teams.</p></div><div className="intro-stats"><span>10,000+ <small>trips tracked</small></span><span>99.9% <small>platform uptime</small></span></div></section><section className="auth-card"><div><p className="eyebrow">WELCOME {register ? 'ABOARD' : 'BACK'}</p><h2>{register ? 'Create your driver account' : 'Sign in to FleetSphere'}</h2><p className="muted">{register ? 'Your organization will approve access after registration.' : 'Use your organization credentials to continue.'}</p></div><form onSubmit={submit}>{register && <label>Full name<input required value={form.name} onChange={(e) => setForm({ ...form, name: e.target.value })} placeholder="Your name" /></label>}<label>Email address<input required type="email" value={form.email} onChange={(e) => setForm({ ...form, email: e.target.value })} placeholder="you@company.com" /></label>{register && <label>Phone<input value={form.phone} onChange={(e) => setForm({ ...form, phone: e.target.value })} placeholder="+91 98765 43210" /></label>}<label>Password<input required minLength="8" type="password" value={form.password} onChange={(e) => setForm({ ...form, password: e.target.value })} placeholder="••••••••" /></label>{error && <p className="form-error">{error}</p>}<button className="primary wide" disabled={saving}>{saving ? 'Please wait…' : register ? 'Create account' : 'Sign in'}</button></form><p className="switch">{register ? 'Already registered?' : 'New driver?'} <Link to={register ? '/login' : '/register'}>{register ? 'Sign in' : 'Create an account'}</Link></p></section></div>; }

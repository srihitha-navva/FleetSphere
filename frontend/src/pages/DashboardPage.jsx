import { useEffect, useState } from 'react'; import { dashboard } from '../services/resourceService'; import StatCard from '../components/StatCard';
const money = (n) => new Intl.NumberFormat('en-IN', { style: 'currency', currency: 'INR', maximumFractionDigits: 0 }).format(n || 0);
export default function DashboardPage() { const [data, setData] = useState(null); const [error, setError] = useState(''); useEffect(() => { dashboard.overview().then((r) => setData(r.data)).catch((e) => setError(e.message)); }, []); if (error) return <div className="error-state">{error}</div>; if (!data) return <div className="page-loader">Loading dashboard…</div>; const availability = data.totalVehicles ? Math.round((data.availableVehicles / data.totalVehicles) * 100) : 0; return <div className="dashboard"><div className="page-heading"><div><p className="eyebrow">LIVE OVERVIEW</p><h2>Fleet performance at a glance</h2></div><span className="date-chip">● Live data</span></div><section className="stats-grid"><StatCard label="Total vehicles" value={data.totalVehicles} icon="▣" /><StatCard label="Available now" value={data.availableVehicles} icon="✓" tone="blue" /><StatCard label="Active drivers" value={data.totalDrivers} icon="♧" tone="purple" /><StatCard label="Open incidents" value={data.openIncidents} icon="!" tone="orange" /></section><section className="dashboard-grid"><article className="panel utilization"><div className="panel-heading"><div><p className="eyebrow">FLEET UTILIZATION</p><h3>Vehicle availability</h3></div><strong>{availability}%</strong></div><div className="bar"><span style={{ width: `${availability}%` }} /></div><div className="legend"><span><i className="dot teal" />Available {data.availableVehicles}</span><span><i className="dot blue" />Assigned {data.assignedVehicles}</span><span><i className="dot orange" />On trip {data.vehiclesOnTrip}</span><span><i className="dot red" />In maintenance {data.vehiclesInMaintenance}</span></div></article><article className="panel"><div className="panel-heading"><div><p className="eyebrow">FINANCIAL PULSE</p><h3>Operating costs</h3></div></div><div className="costs"><p><span>Fuel spend</span><b>{money(data.totalFuelCost)}</b></p><p><span>Maintenance</span><b>{money(data.totalMaintenanceCost)}</b></p><p><span>Other expenses</span><b>{money(data.totalExpenses)}</b></p></div></article><article className="panel"><p className="eyebrow">TRIP OUTCOMES</p><h3>Current movement</h3><div className="movement"><div><b>{data.completedTrips}</b><span>Completed</span></div><div><b>{data.ongoingTrips}</b><span>Ongoing</span></div><div><b>{data.cancelledTrips}</b><span>Cancelled</span></div></div></article><article className="panel highlight"><p className="eyebrow">EFFICIENCY</p><h3>{data.fuelEfficiency.toFixed(1)} km/L</h3><p>Fleet fuel efficiency across {data.totalKilometers.toLocaleString()} km recorded.</p><span>Cost per km: {money(data.costPerKilometer)}</span></article></section></div>; }
